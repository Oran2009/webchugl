# Keep patch files with Unix line endings (LF) for cross-platform compatibility
patches/*.patch text eol=lf

# Shell scripts with Unix line endings
*.sh text eol=lf

# PowerShell scripts with Windows line endings
*.ps1 text eol=crlf
